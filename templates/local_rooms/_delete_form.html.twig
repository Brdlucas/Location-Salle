<form method="post" action="{{ path('app_local_rooms_delete', {'id': local_room.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ local_room.id) }}">
    <button class="btn">Delete</button>
</form>
